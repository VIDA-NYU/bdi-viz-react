apiVersion: apps/v1
kind: Deployment
metadata:
  name: bdiviz-task2
  labels:
    app: bdiviz-task2
spec:
  # Run two copies of the Pod
  replicas: 1
  # Perform rolling updates, starting containers before stopping the old ones
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      # This is how the Deployment recognizes its Pods, has to match the labels
      # of the Pod template
      app: bdiviz-task2
  template:
    metadata:
      labels:
        app: bdiviz-task2
    spec:
      containers:
        # Here's our Flask container
        - name: bdiviz-task2-app
          # Put your own image here
          image: edenwu/bdi-viz-react:task2
          imagePullPolicy: "Always"
          ports:
            # This is the port we'll expose to the internet eventually
            - name: web
              containerPort: 3000
          resources:
            requests:
              cpu: "80m"
              memory: "1Gi"
